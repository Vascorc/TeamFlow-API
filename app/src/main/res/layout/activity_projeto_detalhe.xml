<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- AppBar com Toolbar -->
    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/Theme.ProjetoIndividual.AppBarOverlay">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbarProjeto"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@drawable/side_nav_bar"
            app:popupTheme="@style/Theme.ProjetoIndividual.PopupOverlay" />

    </com.google.android.material.appbar.AppBarLayout>

    <!-- Conteúdo scrollável -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        android:padding="16dp"
        android:layout_marginTop="?attr/actionBarSize"
        android:background="@color/hevy_background">

        <LinearLayout
            android:id="@+id/containerTarefas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="@color/hevy_background">

            <!-- TextView opcional, se quiseres mostrar o nome do projeto no conteúdo -->
            <TextView
                android:id="@+id/textNomeProjetoDetalhe"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Nome do Projeto"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="@color/hevy_blue"
                android:paddingBottom="16dp"
                android:visibility="gone"/>

        </LinearLayout>
    </ScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
